package org.example.model;

public class Car extends Vehicle {
    public Car(String brand, String model, int year, float price, boolean rented, int vehicleId) {
        super(brand, model, year, price, rented, vehicleId);
    }
}
